version: "3.9"
services:
  oracle:
    image: gvenzl/oracle-free:latest
    container_name: oracle_db
    environment:
      ORACLE_PASSWORD: "password1234"
    ports:
      - "1526:1521"
    volumes:
      - oracle_data:/opt/oracle/oradata
    restart: unless-stopped

volumes:
  oracle_data: